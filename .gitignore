# Personal agent overrides (commit settings.json, ignore local overrides)
.claude/settings.local.json

# OS files
.DS_Store
Thumbs.db

# IDE files
.vscode/
.idea/
*.swp
*.swo

# Environment
.env
.env.local

# Local-only docs (superpowers plans/specs) — not for git
docs/superpowers/

# CORTEX session touch records (ephemeral)
.cortex/.touched-*
